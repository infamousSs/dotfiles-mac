if [ -f ~/.zshrc-env ]; then
    source ~/.zshrc-env
fi

if [ -f ~/.zshrc-deps ]; then
    source ~/.zshrc-deps
fi

if [ -f ~/.zshrc-alias ]; then
    source ~/.zshrc-alias
fi

if [ -f ~/.zshrc-func ]; then
    source ~/.zshrc-func
fi

